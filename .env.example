# ─────────────────────────────────────────────────────────
# VacaPlan AI — Environment Variables
# Copy this file to .env and fill in your values.
# NEVER commit .env to version control.
# ─────────────────────────────────────────────────────────

# ── LLM ──────────────────────────────────────────────────
ANTHROPIC_API_KEY=sk-ant-...

# ── Mock Mode ────────────────────────────────────────────
# Set to "true" for local dev (no real API calls or charges)
# Set to "false" for production with real APIs
USE_MOCK_APIS=true

# ── Flight API (Amadeus) ─────────────────────────────────
# https://developers.amadeus.com/
AMADEUS_API_KEY=
AMADEUS_API_SECRET=

# ── Hotel API (Booking.com Affiliate) ────────────────────
# https://developers.booking.com/
BOOKING_USERNAME=
BOOKING_PASSWORD=

# ── Payments (Stripe) ────────────────────────────────────
# Use sk_test_... for development, sk_live_... for production
STRIPE_SECRET_KEY=sk_test_...

# ── Google Calendar OAuth2 ────────────────────────────────
# Scope: https://www.googleapis.com/auth/calendar.freebusy
GOOGLE_CLIENT_ID=
GOOGLE_CLIENT_SECRET=
GOOGLE_ACCESS_TOKEN=
GOOGLE_REFRESH_TOKEN=

# ── Security ──────────────────────────────────────────────
# Random 32-char secret for HMAC booking tokens
BOOKING_HMAC_SECRET=change-me-to-a-random-32-char-string

# OTP to use in mock/dev mode (production uses real SMS/email OTP)
DEV_OTP=123456

# ── Observability (LangSmith) ─────────────────────────────
# https://smith.langchain.com/
LANGCHAIN_TRACING_V2=true
LANGCHAIN_API_KEY=ls__...
LANGCHAIN_PROJECT=vacaplan-ai

# ── Local Services (auto-configured by docker-compose) ───
# Only change if running services outside Docker
REDIS_HOST=redis
CHROMA_HOST=chroma
CHROMA_PORT=8000
OLLAMA_URL=http://ollama:11434/api/embeddings
DATABASE_URL=postgresql://vacaplan:vacaplan@postgres:5432/vacaplan
